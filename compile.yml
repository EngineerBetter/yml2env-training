platform: linux
inputs:
- name: cli-code
outputs:
- name: built-binary
image_resource:
  type: docker-image
  source:
    repository: training-box-docker-registry:5000/student/golang-image
    insecure_registries:
    - "training-box-docker-registry:5000"
run:
  path: cli-code/compile.sh
